# Example repo-level env file for docker-compose
# Copy this to .env in the repo root and edit values as needed.
# NOTE: do NOT commit your real .env — this is just an example.

# Postgres
POSTGRES_USER=iotuser
POSTGRES_PASSWORD=iotpass
POSTGRES_DB=iotdb

# IoT API (overrides or additional vars)
DATABASE_URL=postgresql://iotuser:iotpass@postgres:5432/iotdb
MQTT_BROKER=mosquitto

# Mosquitto (if you plan to bind-mount config)
MOSQ_ALLOW_ANONYMOUS=true
