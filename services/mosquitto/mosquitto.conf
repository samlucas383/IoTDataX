# Listen for MQTT connections on all network interfaces
listener 1883 0.0.0.0

# Optional WebSocket listener (used only if your frontend needs it)
listener 9001 0.0.0.0
protocol websockets

# Allow devices (like your device-sim container) to connect without a password
allow_anonymous true

# Optional but helps when debugging and developing
persistence true
log_type all
